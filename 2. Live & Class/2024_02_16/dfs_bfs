DFS
자료구조 : stack
장점 : 구현이 상당히 간단함(재귀적인 코드로 짜는 경우)
        스택이라는 자료 구조를 사용하기 때문에 메모리 사용량이 적다.
단점 : 최단 경로가 아닌 다른 경로를 결과값으로 찾을 수 가 있다.(최적해가 X)

BFS
자료구조 : queue
장점 : 최단 경로를 보장한다. (최소 이동 횟수도 확인 가능)
단점 : DFS에 비해 메모리 사용이 많다. 
        구현이 다소 복잡하다.

결론 -> 문제에 따라서 DFS, BFS 적절하게 활용

BFS : 최단 경로, 최소 시간
DFS : 탐색할 노드 많거나, 경로 자체가 중요하지 않은 상황